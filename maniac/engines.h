
#ifndef ENGINES_H
#define ENGINES_H

extern struct move_t materialistic_engine(struct los_alamos_t, struct move_checker_t, int);

extern struct move_t random_engine(struct los_alamos_t, struct move_checker_t, int);

#endif
